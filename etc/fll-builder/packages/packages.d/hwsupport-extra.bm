FLL_PACKAGES+=(
	alsa-utils
	dmidecode
	dmsetup
	hwinfo
	laptop-detect
	libsmbios-bin
	lirc
	pm-utils
	powersaved
	vbetool
)

if [[ ${FLL_BUILD_ARCH[${arch}]} == i386 ]]; then
	FLL_PACKAGES+=(
		libc6-i686
	)
fi

